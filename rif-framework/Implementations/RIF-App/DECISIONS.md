# Décisions d'implémentation — RIF-App

**Statut :** Phase 0A en cours.
**Portée :** décisions structurantes propres à l'implémentation RIF-App. Ce registre ne modifie pas et ne remplace pas le registre ADR du Framework (`Framework/99_DOCUMENTATION/99_DECISIONS_ARCHITECTURE.md`) — voir non-objectif §25 du PRD.
**Réfère à :** `RIF APP/PRD_RIF_V2.1_Neon_Vercel.md` (V2.1, 11.09.2026) — référence produit et technique courante depuis D-19. `docs/prd-generateur-rendu-v2.md` (V1.3) reste la spec du code aujourd'hui en place, non encore adaptée.

---

## D-01 — RIF-App est une implémentation du Framework, pas un remplacement

**Statut :** Accepted.
Le Framework RIF (`rif-framework/Framework/`) reste la source unique des règles universelles (matériaux, styles, modes, checklist, cas de test). RIF-App consomme ce Framework via son package d'implémentation versionné (`Implementations/RIF-App/`) et ne modifie jamais les fichiers `Framework/` depuis le code applicatif.

## D-02 — Hébergement et stack technique

**Statut :** Accepted (direction déjà validée avant ce document).
- Frontend + backend applicatif : Next.js 16 (App Router), hébergé sur Vercel.
- Base de données, authentification, stockage : Supabase (Postgres + Auth + Storage).
- Aucun n8n dans le chemin de production de RIF-App — le webhook n8n V2 (`keALNdFoFMNT1Sxk`) est abandonné avec le formulaire V2.

## D-03 — Authentification et RLS

**Statut :** Accepted (principe) — implémentation restant à écrire en Phase 0B.
- Authentification individuelle via Supabase Auth (email + mot de passe pour V1 ; un seul compte réel, Évariste, plus un compte de supervision Alpha_no_code).
- Row Level Security activée sur `dossiers`, `files`, `generations`, `quality_audits`, `events` dès leur création — jamais ajoutée après coup.
- Politique RLS de référence : un utilisateur ne voit que les lignes dont il est `owner_id` (ou rattachées à un dossier dont il est propriétaire) ; un rôle de supervision (Alpha_no_code) a un accès en lecture large via une policy dédiée, jamais via la clé `service_role` exposée côté client.
- Toute clé (`service_role`, fal.ai, LLM) reste côté serveur (Route Handlers / Server Actions Next.js), jamais dans un composant client ni dans ce dépôt.

## D-04 — Stockage et transport des fichiers

**Statut :** Accepted.
- Bucket Supabase Storage privé pour les sources et les rendus.
- La base relationnelle ne stocke que `storage_key` + métadonnées (voir PRD §13.2).
- URLs signées temporaires uniquement, jamais d'URL publique permanente.
- Pas de copie Google Drive dans le chemin critique ; une éventuelle copie intervient après enregistrement du rendu canonique (PRD §12).

## D-05 — Appel fal.ai : direct, sans service intermédiaire séparé

**Statut :** Accepted.
Le PRD (§21) exclut déjà toute dépendance à un compte ChatGPT personnel et impose des appels API directs. Le « service contrôlé par Alpha_no_code » évoqué au §26 point 7 est le **backend applicatif Next.js lui-même** (Route Handlers/Server Actions) : il appelle fal.ai directement avec la clé serveur, sans microservice ou proxy additionnel. Pas de nouvelle brique d'infrastructure à maintenir.
Endpoint retenu (confirmé par inspection du workflow n8n V1 encore en production) : `fal-ai/nano-banana-pro/edit`.

## D-06 — Choix du LLM multimodal

**Statut :** Accepted — 07.09.2026.
**Décision :** Claude Sonnet 5 (`claude-sonnet-5`) pour les trois rôles (dialogue, extraction structurée du ProjectState, analyse assistée des sources). Analyse comparative et chiffrage : `docs/decision-llm-multimodal.md`.
**Conséquences :** secret serveur `ANTHROPIC_API_KEY` ; le coût réel par dossier reste à mesurer en Phase 0B (§20 du PRD) et peut motiver une bascule ponctuelle vers Opus 5 sur l'extraction, ou un modèle moins cher sur le dialogue simple — sans nouvelle décision structurante, tant que la fiabilité aux points critiques est préservée.
**Rappel de gouvernance (PRD §21) :** une mise à jour du modèle déclenche les tests applicables avant mise en production, et chaque génération enregistre la version exacte du modèle utilisé.

## D-14 — Clôture formelle du plan V2 déterministe

**Statut :** Accepted — 07.09.2026.
**Contexte :** le prémortem du 02.09.2026 identifie comme risque #7 le fait que le plan V2 (formulaire 7 étapes, prototype React, schéma Supabase, webhook n8n unique) soit « abandonné de fait sans décision formalisée » — laissant deux projets à moitié faits et aucun plan B lisible.
**Décision :** le plan V2 déterministe est **arrêté**, pas gelé. Le code du formulaire a été retiré du dépôt (commit `d7fbf20`), le webhook n8n V2 (`keALNdFoFMNT1Sxk`) est abandonné, et l'ancien `supabase/schema.sql` est conservé en lecture seule à titre historique.
**Motif, en une phrase :** le client a explicitement rejeté l'expérience formulaire comme « trop contraignante » ; poursuivre les deux voies en parallèle coûterait plus que la valeur d'un plan B dont l'ergonomie est déjà refusée.
**Plan B réel :** ce n'est pas la V2, c'est **GPT RIF (V1), maintenu en production comme filet de sécurité** jusqu'au critère de sortie de la Phase 1 (PRD §23).
**Éléments réutilisables :** le catalogue matériaux Supabase et les prompts de LIB-001 restent exploitables ; leur devenir est à documenter en Phase 0B (case déjà prévue au §23).

---

## Traçabilité du prémortem du 02.09.2026

Le prémortem (`docs/premortem-20260902-transcript.md`, rapport : `docs/premortem-20260902-rapport.html`) est antérieur au PRD V1.3 et l'a largement nourri. État de couverture au 07.09.2026 :

| # | Risque prémortem | Couverture |
|---|---|---|
| 1 | Fiabilité de l'appel d'outil jamais confirmée | **Ouvert** — à traiter dans l'orchestrateur (aucune action ne doit partir d'un texte imitant un appel d'outil) puis mesurer en Phase 0B |
| 2 | Polling fal.ai jamais validé de bout en bout | **Ouvert** — Phase 0B, première case du §23 |
| 3 | Contrôle qualité visuel jamais éprouvé en réel | Cadré et outillé — `lib/rif/controle-qualite.ts` implémente la grille LIB-002 et le garde-fou d'export (§15.3) ; **reste ouvert** : la mesure réelle du taux de faux négatifs (Phase 0B) n'a pas encore de données |
| 4 | Attente de parité stricte avec GPT RIF | Cadré — PRD §22, dernier critère d'acceptation ; profil client `PROFIL_EVARISTE.md` |
| 5 | Aucune infrastructure de production réelle | **Traité** — auth individuelle + RLS (D-03), secrets serveur, journalisation transactionnelle (PRD §18.1) |
| 6 | 60 €/mois jamais recalculé | Cadré — PRD §20 ; chiffrage LLM fait (`docs/decision-llm-multimodal.md`), coût fal.ai à mesurer en Phase 0B |
| 7 | Plan V2 abandonné sans décision | **Traité** — D-14 ci-dessus |
| 8 | Aucun filet de sécurité ni critère de bascule | **Traité** — PRD §23 Phases 1 et 2 (GPT RIF maintenu, seuil de 10 dossiers consécutifs) |

Les trois risques encore ouverts (#1, #2, #3) sont exactement ceux qui exigent une mesure réelle, pas une décision. Ils constituent le cœur de la Phase 0B.

## D-07 — ProjectState : structure de base

**Statut :** Accepted (structure), champs détaillés à compléter au fil de la Phase 0B.
Le schéma suit le PRD §10 et l'exemple `Exemples/PROJECT_STATE_EXEMPLE.json` : chaque donnée significative porte `value`, `status` (`provisional`/`validated`/`rejected`/`unknown`), `source_id`, `authority`, `editable`, `locked`, `validated_by`, `validated_at`, `notes`. Le prompt final est toujours généré depuis une révision immuable et confirmée.

## D-08 — Machine à états : liste et préconditions

**Statut :** Accepted (liste des états, PRD §8) — préconditions détaillées de transition à écrire en Phase 0B dans le code du backend, jamais côté frontend seul.
États : `BROUILLON → SOURCES_REÇUES → SOURCES_CONTRÔLÉES → COLLECTE_EN_COURS → FICHE_À_CONFIRMER → PRÊT_À_GÉNÉRER → GÉNÉRATION_EN_COURS → CONTRÔLE_À_EXAMINER → VALIDÉ | À_CORRIGER | À_REPRENDRE | SUSPENDU | ÉCHEC`.

## D-09 — Trois opérations distinctes, jamais confondues

**Statut :** Accepted.
`genererRendu`, `corrigerRendu` et `reprendreDepuisSources` (PRD §14) sont trois contrats d'API distincts avec des préconditions différentes. Aucune route ne doit permettre de déguiser une reprise en correction locale ou l'inverse.

## D-15 — Extraction structurée du ProjectState via un quatrième outil

**Statut :** Accepted — 07.09.2026.
**Contexte :** le PRD ne spécifie aucun mécanisme pour faire passer les informations de la conversation dans le ProjectState structuré (§10). Sans mécanisme fiable, le risque est le même que celui déjà traité par la garde d'appel d'outil (D-06/appel-outil.ts) : une information donnée en conversation mais jamais réellement enregistrée, avec la même classe de conséquence que le prémortem #1.
**Décision :** ajout d'un quatrième outil, `mettreAJourFicheProjet`, en plus des trois contrats du PRD §14 — soumis à la même garde anti-hallucination (`analyserReponseModele`/`OPERATIONS_RIF`, `lib/rif/appel-outil.ts`) que les trois opérations de génération. Le modèle propose une mise à jour minimale (valeur + statut `provisional`/`validated`) ; le backend (`lib/rif/extraction-project-state.ts`) complète seul les métadonnées de traçabilité exigées par le ProjectState (source_id, authority, editable, locked, validated_at) — le modèle décide QUOI, jamais COMMENT c'est tracé.
**Règle de non-régression (choix d'implémentation, pas une règle citée du PRD) :** une valeur déjà `validated` n'est jamais dégradée par une mise à jour `provisional` ultérieure ; les mises à jour ignorées à ce titre ne sont jamais silencieuses, elles sont retournées et journalisées (`fiche_projet_mise_a_jour`).
**Limite explicite :** cette opération ne touche jamais la `revision` du ProjectState — la révision n'avance qu'à la confirmation explicite de la fiche projet (PRD §9.4), dont le câblage (transition FICHE_À_CONFIRMER → PRÊT_À_GÉNÉRER avec incrément de révision) reste à faire.
**Autorisation :** `mettreAJourFicheProjet` n'est licite que tant que la fiche n'est pas confirmée (`BROUILLON` à `FICHE_À_CONFIRMER`) — refusée à partir de `PRÊT_À_GÉNÉRER`.

## D-16 — Avancement du parcours proposé par le modèle, décidé par le backend

**Statut :** Accepted — 08.09.2026.
**Contexte :** aucun code ne faisait avancer un dossier au-delà de `BROUILLON`. Le graphe de transitions et `verifierPrecondition` existaient depuis la Phase 0A, mais sans appelant : la fiche projet n'atteignait jamais `FICHE_À_CONFIRMER`, donc le bouton de confirmation (§9.4) n'apparaissait jamais et la génération était structurellement inatteignable.
**Décision :** un cinquième outil, `avancerParcours`, applique littéralement le PRD §8 (« Le LLM peut proposer une transition ; seul le backend l'applique ») : le modèle propose un état cible et un motif, `autoriserAvancementParcours` (`lib/rif/appel-outil.ts`) tranche.
**Bornes :** le modèle ne peut proposer que `SOURCES_CONTROLEES`, `COLLECTE_EN_COURS`, `FICHE_A_CONFIRMER` et `SUSPENDU` (§9.2, blocage majeur). `PRET_A_GENERER` est explicitement exclu — §9.4 exige une action volontaire d'Évariste. Les états de production (`GENERATION_EN_COURS`, `CONTROLE_A_EXAMINER`, `VALIDE`, `A_CORRIGER`, `A_REPRENDRE`, `ECHEC`) découlent des opérations et de l'audit qualité, jamais d'une proposition conversationnelle.
**Exception mécanique :** `BROUILLON → SOURCES_REÇUES` se fait au dépôt de la première source, sans passer par le modèle — c'est un fait constaté (des sources sont arrivées), pas un jugement.

## D-17 — Couche Contraintes & Libertés

**Statut :** Accepted — 08.09.2026 (PRD V1.3 §9.4A, §24A).
**Décision :** la matrice est portée par le ProjectState (`contraintes_libertes`, clé = élément) et gardée par `lib/rif/contraintes-libertes.ts`. Trois règles du PRD y sont implémentées littéralement :
- §7.3 — « La liberté créative n'est jamais implicite » : une propriété absente de la matrice vaut `locked`, jamais libre. Le défaut est la fidélité.
- §24A.4 — « la propriété la plus restrictive prévaut » : la politique effective est le minimum entre la politique déclarée et le `freedom_level` de l'élément.
- §24A.1 — la géométrie d'un élément structurel (caméra, cadrage, perspective, silhouette, volumes, ouvertures, toiture, implantation, piscine, terrasse, annexe, environnement conservé) ne descend jamais sous `strict`, même si la matrice l'y autorise. Une demande d'embellissement ne peut donc pas desserrer une propriété structurelle.

**Conséquence sur la génération :** `prompt-technique.ts` inscrit dans les deux prompts (génération et correction) les contraintes structurelles, les libertés réellement accordées et les présences autorisées, avec la mention explicite que toute liberté non listée est refusée — le moteur ne comble plus le silence par de l'invention.

**Non fait :** le Generation Package structuré du §9.5 (objet portant prompt + sources + zones + directives + contraintes + libertés) n'existe pas encore comme structure ; les contraintes voyagent aujourd'hui dans le texte du prompt. À reprendre quand la matrice de capacités réelles de l'endpoint image (§23 Phase 0B) dira ce que le moteur sait consommer autrement que du texte.

**§24A.3 câblé (09.09.2026) :** « la proposition est confirmée avant génération lorsqu'elle modifie une liberté précédemment absente ». Une politique `controlled`/`creative` déclarée sans `authorized_by` n'a aucun effet (`politiqueEffective` la traite comme `locked`) ; un durcissement (`locked`/`strict`) s'applique immédiatement, sans autorisation. Le modèle peut désormais proposer des entrées `contraintes_libertes` via `mettreAJourFicheProjet` (schéma étendu dans `prompt-conversationnel.ts`) — `appliquerMiseAJourFicheProjet` lui interdit structurellement d'y inscrire `authorized_by`/`authorized_at`, et fait repartir en attente (`elargitLaLiberte`) une autorisation déjà accordée si la proposition l'élargit. `POST .../confirmer` — seule action humaine du parcours (§9.4) — appelle `autoriserLibertesEnAttente` avant d'incrémenter la révision : c'est le seul endroit qui transforme une proposition en liberté effective. `FicheProjet.tsx` liste les libertés en attente (`libertesEnAttente`) pour qu'Évariste les voie avant de confirmer.

## D-18 — Nommage des opérations : genererRendu / corrigerRendu

**Statut :** Accepted — 08.09.2026, aligné code et PRD.
Le suffixe « Flux » des noms d'origine (`genererRenduFlux`, `corrigerRenduFlux`) prêtait à confusion avec le modèle Flux, qui n'est pas le moteur retenu (D-05 : `fal-ai/nano-banana-pro/edit`). Les opérations s'appellent `genererRendu` et `corrigerRendu` dans le code (commit `b2fecde`) comme dans le PRD §14. Les documents figés — prototype de référence, transcription du prémortem, copie du PRD dans `rif-framework/Documentation/` — gardent l'ancien nom : ce sont des témoins d'un état daté, pas la spec vivante.

## D-19 — Bascule sur le PRD V2.1 : référence produit, mode principal et stack cible

**Statut :** Accepted — 12.09.2026. Décision documentaire uniquement : **aucun code TypeScript n'a encore été modifié à ce titre**.

**Décision :**
- `RIF APP/PRD_RIF_V2.1_Neon_Vercel.md` (V2.1) devient la référence produit et technique de RIF-App, remplaçant `docs/prd-generateur-rendu-v2.md` (V1.3) à ce titre. Le code actuel reste bâti sur la V1.3 tant que l'implémentation n'a pas repris ce chantier (voir « Ce qui reste à construire », CLAUDE.md).
- Le Framework a été recalibré en conséquence, V1.4 → V1.6 (ADR-019, ADR-020, `Framework/99_DOCUMENTATION/99_DECISIONS_ARCHITECTURE.md`) : `retexturation_contextualisee` devient le mode principal du cas standard d'Évariste (LIB-006 §4) ; le modèle environnemental officiel utilise trois états `locked`/`editable`/`harmonizable`, définis une seule fois dans ARCH-002 (`00_VOCABULAIRE_SYSTEME.md`) ; les styles sont `presentation_naturelle`/`administratif_sobre`/`commercial` (LIB-005). Ce registre ne recopie pas ces règles — s'y référer directement plutôt qu'ici.
- Cible technique produit (PRD V2.1 §16) : Neon PostgreSQL + Vercel Blob, authentification mono-utilisateur, accès base exclusivement serveur. Supabase sort de la cible.
- État réel constaté (audit du 12.09.2026, non joint ici) : aucun projet Supabase n'a jamais été connecté, même en local (`.env.local` ne porte aucune variable Supabase) — **Supabase ne contient aucune donnée réelle**. La migration Neon/Vercel Blob est donc un changement de cible, pas une migration de données ; `lib/rif/depot.ts` (interface `DepotDossiers`) découple déjà la logique métier de Supabase, ce qui borne le risque technique de la bascule.
- **Aucun développement TypeScript du nouveau modèle (mode, environnement, styles, Neon, Vercel Blob) n'a commencé.** Le code présent dans ce dépôt implémente encore le Framework V1.4 / PRD V1.3.

**Conséquences :** la liste « Ce qui reste à construire » de `CLAUDE.md` et l'inventaire des fichiers TypeScript à adapter (`ModeProduction`, `modes-styles.ts`, `collecte-conditionnelle.ts`, `prompt-technique.ts`, `contraintes-libertes.ts`, `controle-qualite.ts`, `StyleRendu`, `depot-supabase.ts`) font foi sur le périmètre exact restant, pas ce registre.

**Mise à jour — 16.09.2026 :** le domaine métier TypeScript a depuis été aligné sur le PRD V2.1, en trois lots successifs :
1. `ModeProduction`/`StyleRendu`/modèle d'environnement (`ElementEnvironnement`) — voir `lib/rif/project-state.ts`, `modes-styles.ts`, `collecte-conditionnelle.ts`, `prompt-technique.ts`, `contraintes-libertes.ts`, `controle-qualite.ts`, `prompt-conversationnel.ts`, `extraction-project-state.ts` ;
2. machine à états renommée (`lib/rif/etat-machine.ts`) : `SOURCES_CONTRÔLÉES`+`COLLECTE_EN_COURS` → `SOURCES_ANALYSÉES`, `FICHE_À_CONFIRMER` → `CONTEXTE_À_CONFIRMER` (12 états, PRD V2.1 §17) — répercuté dans `appel-outil.ts`, `orchestrateur.ts`, `orchestrateur-conversationnel.ts`, `prompt-conversationnel.ts`, la Route Handler `confirmer`/`reprendre`, la page `app/dossiers/[dossierId]/page.tsx` et la contrainte `check` de `supabase/schema-rif-app.sql` (jamais exécutée en réel — aucune donnée à migrer).

Les anciens noms d'état cités par D-08 et D-16 ci-dessus ne sont donc plus ceux du code — ces deux décisions ne sont pas réécrites (traçabilité) ; se référer à `lib/rif/etat-machine.ts` pour la liste et le graphe actuels.

**Mise à jour — 17.09.2026 (bascule réelle) :** projet Neon réel créé et lié (`falling-feather-95511005`, région `aws-eu-west-2`, Postgres 18, branche `production`), via la séquence CLI fournie par l'utilisateur (`neon login`/`skills`/`mcp`/`link`/`config init`/`deploy`) — vérifiée légitime (`npm view neon`) avant exécution, chaque étape à risque confirmée par l'utilisateur. `neon.ts` déclare `auth: true`. Correction apportée à `neon/schema.sql` avant exécution : Neon Auth (Managed Better Auth) provisionne lui-même `neon_auth."user"` — la table `users` maison de la première version était donc redondante, retirée ; `owner_id`/`validated_by`/`actor_id` référencent `neon_auth."user" (id)`. Schéma exécuté avec succès sur la branche `production` réelle (11 instructions). `obtenirDepot()` (`_lib/reponse.ts`) appelle désormais `creerDepotNeon` — plus `creerDepotSupabase`. Authentification : `lib/auth/{server,client}.ts` (`@neondatabase/auth`, API vérifiée sur le `llms.txt` embarqué du paquet et ses `.d.mts`, pas sur un souvenir d'entraînement) ; `proxy.ts` délègue à `auth.middleware()` (exclut `/api/*` du matcher — comportement de repli différent de Supabase, à confirmer en usage réel : pas de garantie documentée que Neon Auth préserve `?redirect=` au retour de connexion) ; `app/connexion/page.tsx` appelle `authClient.signIn.email`. Vérifié en conditions réelles contre le service Neon Auth hébergé (`x-neon-ret-request-id` présent sur `/api/auth/get-session`), pas seulement en test unitaire. `lib/supabase/*`, `depot-supabase.ts` et `supabase/schema-rif-app.sql` n'ont plus aucun appelant — conservés pour l'instant, retrait proposé en `CLAUDE.md` une fois la bascule validée en usage réel. 223 tests passent (inchangé), tsc/build/lint propres, `npm audit` : 0 vulnérabilité.

**Mise à jour — 17.09.2026 (compte initial) :** compte d'Évariste Blasco créé dans Neon Auth via `POST /api/auth/sign-up/email` (one-off, email fourni par l'utilisateur) — `neon_auth."user"` porte désormais 1 ligne (`55f8f679-7f5d-4e42-a1fe-394d8fe44dcc`). La connexion via `app/connexion/page.tsx` est donc utilisable de bout en bout.

**Mise à jour — 17.09.2026 (retrait du code Supabase de RIF-App) :** la bascule validée, le code Supabase propre à RIF-App est supprimé (pas seulement laissé mort) : `lib/supabase/{client,server}.ts`, `lib/rif/depot-supabase.ts` (+ test), `supabase/schema-rif-app.sql`, `docs/provisioning-supabase.md` ; dépendances `@supabase/ssr`/`@supabase/supabase-js` désinstallées ; bloc Supabase retiré de `.env.example` (aucune variable Supabase n'était de toute façon définie en réel) ; mentions résiduelles dans les commentaires de `lib/rif/{claude-client,etat-machine,orchestrateur,orchestrateur.test,depot}.ts`, `lib/auth/server.ts`, `lib/neon/client.ts`, `lib/storage/vercel-blob.ts` et `neon/schema.sql` reformulées pour ne plus référencer Supabase. Ce retrait ne concerne que le code de RIF-App lui-même — pas `supabase/schema.sql` ni les artefacts du plan V2/n8n déjà arrêté (D-14), qui restent une décision distincte.

**Mise à jour — 17.09.2026 (persistance de l'historique de conversation, PRD §13.5) :** nouvelle table `messages` (`neon/schema.sql`, exécutée sur la branche `production` réelle) — `role`/`content` texte seul, jamais les blocs `tool_use`/`tool_result` bruts de l'API Anthropic (choix délibéré : évite d'avoir à réinjecter un `tool_result` pairé pour rester valide vis-à-vis de l'API au tour suivant, ce qui aurait fait basculer ce lot dans la « boucle multi-tour complète », non demandée ici). `DepotDossiers` gagne `obtenirHistoriqueConversation`/`ajouterMessageConversation` ; `executerTourConversationnel` (`lib/rif/orchestrateur-conversationnel.ts`) charge l'historique depuis le dépôt au lieu de le recevoir de l'appelant, et persiste chaque tour via un point de sortie unique (`finaliser`) appliqué à tous les `return` — `resumerTour` calcule le texte une seule fois, partagé entre la persistance et l'affichage client. `app/api/dossiers/[dossierId]/message/route.ts` gagne un GET (historique) ; le POST n'accepte plus `historique` dans le corps. `ConversationRif.tsx` recharge l'historique au montage au lieu de le garder en state React perdu au rechargement de page. Vérifié en conditions réelles : table créée sur la base Neon réelle, écriture/lecture ordonnée confirmée par SQL direct et par la route GET, dossier de test nettoyé ensuite. 221 tests passent (+1), tsc/build/lint propres, 0 vulnérabilité.

**Mise à jour — 17.09.2026 (upload direct navigateur → Vercel Blob, PRD §16.1) :** la production Vercel renvoyait `413 Content Too Large` sur `POST .../sources` — les Vercel Functions plafonnent le corps d'une requête à 4,5 Mo, incompatible avec des exports Revit ou rendus réels transitant en `multipart/form-data`. Le flux ne fait plus jamais transiter le binaire par une Function :

1. `app/api/dossiers/[dossierId]/sources/token/route.ts` (nouveau) — émet un jeton client Vercel Blob (`handleUpload` de `@vercel/blob/client`) après authentification + vérification de propriété du dossier ; le jeton est contraint (type MIME, taille max 25 Mo, portée limitée au dossier via le préfixe de pathname, validité 5 min) — jamais le jeton maître `BLOB_READ_WRITE_TOKEN`.
2. `DepotSources.tsx` — téléverse directement au navigateur vers Vercel Blob avec ce jeton (`upload()`), puis appelle `.../sources` avec un petit JSON (`pathname`, `originalName`, `role`) — plus de `FormData`/binaire dans le corps.
3. `app/api/dossiers/[dossierId]/sources/route.ts` — relit le blob déjà déposé (`lireFichier`) pour la détection de rôle (qui a besoin des octets réels), revalide type/taille réels (jamais la déclaration du client), puis persiste les métadonnées. Un échec après upload (détection, Neon) supprime le blob orphelin (`supprimerFichier`) plutôt que de le laisser facturé sans usage.

`lib/storage/contraintes-source.ts` (nouveau, testé) centralise les constantes (MIME acceptés, taille max) et la validation de pathname, partagées par les deux routes et le client — sans dépendre de `@vercel/blob` (évite d'embarquer le SDK serveur dans le bundle navigateur). `DepotDossiers.enregistrerFichierSource` (`lib/rif/depot.ts`, `depot-neon.ts`) change de contrat : reçoit `storageKey`/`sizeBytes` (upload déjà fait) au lieu de `contenu`/`ownerId` — n'appelle plus `televerserFichier`, seulement l'insertion `files`. Schéma `files` inchangé. `enregistrerFichierResultat` (rendus fal.ai) non touché : c'est un fetch serveur→serveur, jamais soumis à la limite de corps d'une Function. 225 tests passent (+4, `lib/storage/contraintes-source.test.ts`), tsc/build/lint propres, 0 vulnérabilité.

**Mise à jour — 17.09.2026 (parsing robuste des réponses API côté client) :** symptôme observé en production après les 413 de l'upload : « Unexpected end of JSON input » affiché à l'utilisateur. Cause exacte : `DepotSources.tsx` faisait `await reponse.json()` sans jamais vérifier `reponse.ok` ni la présence d'un corps — un 413 posé par la plateforme Vercel (avant que notre code s'exécute, corps vide ou non-JSON) ou tout échec d'infrastructure faisait planter `JSON.parse` avec un message brut de moteur JS, jamais rattrapé par notre gestion d'erreur métier (le throw arrivait avant la vérification `success`). `lib/rif/reponse-client.ts::lireReponseApi` (nouveau, testé) centralise la lecture : 413 → message dédié, corps vide → message HTTP générique, JSON invalide → message générique, `success:false` → message métier relayé — jamais l'erreur de parsing brute. Câblé sur les 3 appels fetch de `DepotSources.tsx` (confirmation de rôle, confirmation de directive, dépôt de source). **Même bug latent identifié mais non corrigé** (hors périmètre de ce lot) dans `BoutonNouveauDossier.tsx`, `BoutonConfirmerFiche.tsx`, `ConversationRif.tsx` (×2), `VerdictQualite.tsx` — même motif `await reponse.json()` sans garde. 231 tests passent (+6), tsc/build/lint propres.

**Mise à jour — 17.09.2026 (migrations SQL versionnées) :** audit read-only préalable (non modifié ici) : le schéma Neon réel était 100 % conforme à `neon/schema.sql`, aucun SQL fonctionnel manquant — le seul problème était l'absence de tout système de suivi (`neon/schema.sql` appliqué une fois, à la main, sans trace). Mis en place :

- `neon/migrations/0001_initial_schema.sql` — copie exacte de l'ancien `neon/schema.sql` (vérifié identique par introspection avant copie), qui n'est plus lui-même la source de vérité (contenu remplacé par une note de redirection, jamais supprimé pour ne pas casser un lien).
- `lib/neon/migrations.ts` (+ `.test.ts`) — logique pure et injectable (ordre, non-répétition, arrêt sur échec), testée sans base réelle, comme le reste du dépôt.
- `lib/neon/migrations-executeur.ts` — traduction SQL : chaque migration s'applique dans **une seule transaction HTTP** (`sql.transaction`) avec son enregistrement dans `schema_migrations` — jamais l'un sans l'autre, jamais de migration à moitié appliquée.
- `scripts/db-migrate.ts` — exécuté directement via Node (support TypeScript natif, Node 24 ; `tsconfig.json` gagne `allowImportingTsExtensions: true` pour permettre les imports relatifs avec extension `.ts` qu'exige le résolveur ESM natif de Node — sans emit, sans risque pour le reste du projet). `npm run db:migrate` / `npm run db:migrate:status`. **Non connecté au build Vercel**, comme demandé.
- Vérifié en conditions réelles (lecture seule au sens métier) : `npm run db:migrate:status` contre la base Neon réelle crée la table `schema_migrations` (si absente) et rapporte correctement `0001_initial_schema.sql` en attente — **je n'ai pas exécuté `db:migrate` pour de vrai** (qui marquerait `0001` comme appliquée) : geste réel sur la prod, laissé à une décision explicite malgré son idempotence démontrée.

240 tests passent (+9), tsc/build/lint propres, `npm audit` : 0 vulnérabilité.

**Mise à jour — 17.09.2026 (première application réelle + durcissement de l'exécuteur) :** `npm run db:migrate` exécuté contre la branche `production` réelle — `0001_initial_schema.sql` appliquée, `schema_migrations` en contient la trace (`version: '0001'`), comptages des tables métier vérifiés identiques avant/après (aucune donnée touchée).

Dette identifiée juste après : `migrations-executeur.ts` découpait les fichiers de migration sur `;`, fragile pour tout ce qui n'est pas du DDL trivial (`DO $$…$$`, `CREATE FUNCTION`/`TRIGGER`, chaîne contenant un `;`). Cause exacte : le driver HTTP `neon()` (utilisé partout ailleurs dans l'app) n'exécute qu'une seule instruction paramétrée par appel (protocole « extended query » de Postgres) — découper était la façon de s'y plier. Corrigé en basculant `appliquerUne` sur le **`Client` WebSocket** du même paquet `@neondatabase/serverless` : une vraie session Postgres (BEGIN/COMMIT/ROLLBACK réels), qui exécute le fichier de migration **tel quel** via le protocole « simple query » — Postgres fait sa propre analyse syntaxique, plus de découpage maison. `decouperInstructions` retirée de `lib/neon/migrations.ts` (obsolète, plus utilisée). Vérifié en direct contre Neon réel avant d'écrire le code définitif : `DO $$...$$`, `CREATE FUNCTION`, `CREATE TRIGGER`, chaîne contenant `;`, commentaire — tout fonctionne nativement ; `ROLLBACK` explicite confirmé sans laisser aucune trace (fonction/trigger de test absents après coup). `creerTableSuivi`/`obtenirMigrationsAppliquees` restent sur le driver HTTP léger (instructions uniques, non paramétrées — jamais concernées par la limite). `lib/neon/migrations-executeur.test.ts` (nouveau, `Client` mocké) : le fichier de migration est passé verbatim à `client.query` (jamais réécrit), séquence `BEGIN`/migration/insert/`COMMIT` exacte, `ROLLBACK` + non-écriture de `schema_migrations` + connexion toujours fermée en cas d'échec. Aucun SQL production exécuté pendant ce lot (hors la validation manuelle, sur des objets de session éphémères explicitement annulés). 243 tests passent (+3), tsc/build/lint propres, `npm audit` : 0 vulnérabilité.

**Toujours non commencé / ouvert :**
- Même bug de parsing JSON non gardé que `DepotSources.tsx` avait, présent dans `BoutonNouveauDossier.tsx`, `BoutonConfirmerFiche.tsx`, `ConversationRif.tsx`, `VerdictQualite.tsx` — `lib/rif/reponse-client.ts::lireReponseApi` existe déjà, il ne reste qu'à les brancher dessus.
- `BLOB_READ_WRITE_TOKEN` absent de `.env.local` (dev local) — annoncé provisionné côté Vercel Production, non vérifié depuis cet environnement (pas d'accès dashboard/CLI Vercel).
- La clé de compte Neon minée par `neon mcp -y` (`neon-cli-mcp-20260917T112430Z-7bd4`, id `3344051`) porte un périmètre très large (tout ce que le compte peut atteindre, toutes organisations) — signalé à l'utilisateur avec la commande de révocation (`neon api-keys revoke 3344051`), jamais révoquée depuis cet environnement.

**Mise à jour — 17.09.2026 :** `lib/rif/depot-neon.ts` + `neon/schema.sql` livrés — additifs, non branchés. Correction au passage : `lib/storage/vercel-blob.ts` affirmait qu'aucun équivalent à `createSignedUrl` (Supabase) n'existait côté Vercel Blob — faux, `issueSignedToken`+`presignUrl` composent la même garantie (`resolverUrlSignee`, ajouté). Schéma Neon : mêmes 5 tables que Supabase, sans RLS (PRD §16.3, plus d'accès navigateur direct à contrer) ni `profiles`/rôle de supervision (PRD §16.5, « aucun système de rôles complexe » — lu littéralement, D-03 ne s'applique plus tel quel côté Neon) ; `auth.users` remplacé par une table `users` minimale, non semée. `depot-neon.ts` traite `ownerId`/`actorId`/`validatedBy` comme des identifiants opaques — l'authentification mono-utilisateur qui les établira est un lot distinct, non tranché ici. 223 tests passent (+4), tsc et build propres.

**Mise à jour — 17.09.2026 :** `lib/storage/vercel-blob.ts` livré (`@vercel/blob` 2.8.0) — `televerserFichier`/`lireFichier`/`supprimerFichier` en accès `private`, testés (mock du SDK). Additif, non branché sur `DepotDossiers` ni aucune route : Supabase Storage reste le stockage réellement utilisé. Point d'architecture identifié et documenté en tête du fichier, à trancher avec `depot-neon.ts` : `@vercel/blob` n'offre pas d'équivalent direct à `createSignedUrl` (Supabase) pour donner au navigateur un lien de lecture temporaire ; `presignUrl` existe côté SDK mais suppose un jeton de délégation (`issueSignedToken`) conçu pour l'upload direct client, pas pour une lecture ponctuelle — la voie retenue par défaut est une Route Handler qui lit le blob côté serveur et le retransmet. 219 tests passent (+5), tsc et build propres. Aucune vulnérabilité introduite (`npm audit` : les 5 alertes existantes viennent de `next`/`eslint`, préexistantes, sans rapport avec ce module).

## D-20 — Workflow geometry-first : source modèle 3D, extraction, pack de contraintes (RIF V2)

**Statut :** Accepted — 18.09.2026.

**Contexte :** pivot produit — le moteur d'image ne doit plus être seul responsable de reconstruire la géométrie du projet quand une maquette numérique 3D existe. Voir ADR-021 (`Framework/99_DOCUMENTATION/99_DECISIONS_ARCHITECTURE.md`) pour la décision Framework elle-même (hiérarchie d'autorité, vocabulaire, éléments non tranchés) — ce registre ne la recopie pas, il documente uniquement les choix d'implémentation qui en découlent.

**Autorisation exceptionnelle pour cette mission :** contrairement à D-01 (« RIF-App ne modifie jamais `Framework/` depuis le code applicatif »), l'utilisateur a explicitement autorisé, pour cette mission seulement, la modification de `rif-framework/Framework/` afin d'y intégrer le workflow geometry-first (ADR-021 + REF-001, ARCH-002, LIB-006, LIB-002, `FRAMEWORK_VERSION.md`, `99_CHANGELOG.md`, `SHA256SUMS.txt` régénéré). Le dossier non suivi `RIF APP/` n'a jamais été modifié — lu uniquement comme référence pour le PRD. D-01 reste la règle par défaut pour toute mission future ; cette autorisation ne vaut pas précédent implicite.

**Décisions d'implémentation :**
1. `lib/rif/geometrie-3d.ts` (nouveau) porte tout le domaine geometry-first : `AUTORITE_PAR_ROLE_SOURCE`, `SourceModele3D` (7 statuts d'extraction), `GeometryConstraintPack` (schéma versionné, `schemaVersion: 1`), `AlignementCameraPhoto`, abstraction `GeometryExtractor` + `creerExtracteurNonConfigure()` — refuse honnêtement toute extraction tant qu'aucun fournisseur n'est choisi (`EXTRACTION_PROVIDER_NOT_CONFIGURED`), ne simule jamais un résultat géométrique. `CategorieCorrection` (6 valeurs : MATERIAL/LIGHTING/VEGETATION/LOCAL_ENVIRONMENT/ARTIFACT_REMOVAL/MINOR_PRESENTATION) + validateur, pour préserver la géométrie lors d'une correction locale — **type et validateur livrés, non encore câblés** dans `prompt-conversationnel.ts`/`orchestrateur-conversationnel.ts` (un câblage partiel n'apporterait pas la garantie réelle recherchée ; reporté).
2. `ProjectState` (`lib/rif/project-state.ts`) gagne trois champs optionnels — `modele3D`, `geometryPack`, `alignment` — et le rôle de source `model_3d`. **Aucune migration de base de données requise** : ces champs vivent dans la colonne JSONB `project_state` déjà existante, comme le reste du ProjectState. Une migration ne sera nécessaire que si un futur fournisseur d'extraction exige un suivi de job asynchrone durable au-delà de ce que JSONB peut porter raisonnablement.
3. Pipeline de dépôt dédié pour la source 3D, distinct de `.../sources` : `lib/storage/contraintes-modele-3d.ts` (500 Mo max, pas de restriction de type — format non figé), `app/api/dossiers/[dossierId]/modele-3d/token/route.ts` (jeton Vercel Blob) et `.../modele-3d/route.ts` (confirmation). Pas de classification automatique de rôle ici (`detection-role.ts` ne s'applique pas) : ce n'est pas une image, et le rôle est certain par construction (l'utilisateur dépose explicitement dans le slot « Modèle 3D »).
4. Frontend minimal : `DepotSources.tsx` gagne un 4e bloc (Modèle 3D) affichant honnêtement l'état d'extraction (« Extraction non disponible » tant qu'aucun fournisseur n'est configuré) — pas de fausse progression.
5. **Correction réconciliée au passage :** `ProjectState.canonical_result_id`, déclaré depuis un lot antérieur mais jamais lu ni écrit, est désormais synchronisé avec `generations.is_canonical` (mécanisme DB déjà en place) : `definirGenerationCanonique` (`lib/rif/depot.ts`/`depot-neon.ts`) met à jour les deux représentations dans la même transaction.

**Tension terminologique non tranchée (voir ADR-021 point 7) :** `ModeProduction` (`lib/rif/project-state.ts`) conserve ses 3 valeurs actuelles (`retexturation_revit`/`retexturation_contextualisee`/`photomontage_controle`) — **aucun renommage, aucun ajout**. Les trois usages conceptuels du workflow geometry-first (retexturation fidèle, insertion en site réel, présentation à liberté visuelle assumée) ne sont pas mappés 1:1 sur ces 3 valeurs : le troisième usage évoque le mode « Présentation générative », retiré par ADR-019 avec un motif documenté (aucun dossier réel, pas de modèle d'environnement qualifié). Renommer ou réintroduire un mode sur cette seule base romprait une décision produit récente et délibérée sans validation d'Évariste — reporté explicitement au rendez-vous.

**Éléments non tranchés, en attente du rendez-vous Évariste (ne pas choisir depuis le code) :** format 3D définitif (RVT vs IFC/OBJ/FBX), fournisseur d'extraction (Autodesk Model Derivative, Revit Automation, parseur IFC ou autre — aucun choisi), coût et latence réels, version de Revit requise le cas échéant, correspondance exacte usages geometry-first ↔ `ModeProduction`.

**Mise à jour — 18.09.2026 (suite de mission : catégories de correction, audit du backend loop précédent, contrats V2) :**

1. **`CategorieCorrection` câblée de bout en bout.** L'outil conversationnel `corrigerRendu` (`lib/rif/prompt-conversationnel.ts`) exige désormais `categorie` (enum des 6 valeurs, `required`) ; `orchestrateur-conversationnel.ts` la valide via `estCategorieCorrectionValide` avant de construire le prompt technique — toute valeur hors des 6 catégories (y compris une catégorie qui évoquerait un changement architectural) est refusée avec un message explicite renvoyant vers `reprendreDepuisSources`, jamais traitée comme une correction locale silencieuse. Même garde côté route HTTP directe (`app/api/dossiers/[dossierId]/corriger/route.ts`). `lib/rif/prompt-technique.ts::ParametresCorrection` porte `categorie` (obligatoire) et l'inscrit explicitement dans le prompt avec un rappel que la géométrie verrouillée reste inchangée. **Aucune 7e catégorie `ARCHITECTURAL_CHANGE` n'a été ajoutée** — le refus de toute valeur hors des 6 catégories documentées EST le mécanisme qui empêche ce cas, conformément à ADR-021 (ne pas inventer une catégorie qui la légitimerait).
2. **Audit du MASTER BACKEND COMPLETION LOOP précédent** (lecture seule, par sous-agent, puis vérifié) : DONE — historique des générations, lifecycle par génération, ownership (16 routes vérifiées), boucle tool_use/tool_result. PARTIAL — résultats non rechargés après refresh (aucun composant n'appelle `.../generations` au montage), génération canonique et comparaison ont un backend complet mais aucune UI, rapport qualité multimodal implémenté et branché en route mais jamais déclenché par le frontend ni vérifié en conditions réelles, format d'erreur incohérent sur `message/route.ts` et `dossiers/route.ts` (pas de `requestId`), agrégats du dashboard (`DossierResume`) récupérés mais partiellement affichés. NOT_DONE — aucun fichier de contrats backend n'existait. Aucun item n'est `SUPERSEDED_BY_GEOMETRY_FIRST` (tous orthogonaux au pivot géométrique).
3. **Corrections apportées suite à l'audit, dans ce lot :** format d'erreur uniformisé sur `app/api/dossiers/[dossierId]/message/route.ts` et `app/api/dossiers/route.ts` (passent désormais par `repondreErreur`/`repondreDossierIntrouvable`, gagnent un `requestId`) ; `app/dossiers/page.tsx` affiche désormais `nombreGenerations`/présence d'une génération canonique (données déjà récupérées, jamais affichées) ; `FicheProjet.tsx` affiche le statut du modèle 3D, du pack de contraintes et de l'alignement (honnête, jamais de fausse progression).
4. **Corrections délibérément NON apportées dans ce lot** (frontend au-delà du périmètre « minimal » de cette mission, documentées comme gaps explicites dans `docs/RIF_BACKEND_CONTRACTS_V2.md` §17, pas oubliées) : rechargement de `.../generations` après refresh, UI de comparaison, badge canonique sur la page dossier, déclenchement frontend de l'audit multimodal, câblage de `lireReponseApi` sur les 4 composants restants.
5. **`docs/RIF_BACKEND_CONTRACTS_V2.md` créé** (git-tracké) : décrit toutes les routes réelles, le format d'erreur, les états frontend exploitables, et documente explicitement les gaps ci-dessus plutôt que de les taire — objectif : que Codex construise le frontend sans deviner un contrat inexistant.
6. **Migrations :** aucune migration SQL créée dans ce lot — confirmé, tous les champs geometry-first restent dans la colonne JSONB `project_state` existante (voir plus haut). Rien à appliquer en production.

## Décisions encore ouvertes (issues du PRD §26, non couvertes ci-dessus)

- [ ] D-10 — limite maximale de variantes incluses par dossier ou par perspective.
- [ ] D-11 — format technique définitif des directives localisées (table dédiée vs. `project_state` seul) — PRD §13.6 laisse les deux options ouvertes pour la Phase 0.
- [ ] D-12 — stratégie économique de repli si le coût complet observé dépasse 60 €/mois (PRD §20).
- [ ] D-13 — modalités contractuelles finales (hors périmètre technique, le contrat signé fait autorité).

---

## Note — dérive documentaire relevée (Framework, pas RIF-App)

En implémentant ENG-004 (`lib/rif/collecte-conditionnelle.ts`), ENG-004 lui-même référence « REF-001 §9 » pour la règle de lumière suivant la photographie. Dans la version actuelle de REF-001 (01_REFERENTIEL_GENERAL.md), cette règle est en réalité au §6 (« Environnement et lumière ») ; le §9 actuel est « Confidentialité ». Numérotation vraisemblablement décalée par une révision antérieure de REF-001 sans mise à jour de la référence croisée dans ENG-004. Le code s'appuie sur le contenu réel (§6), pas sur le numéro cité. Signalé ici pour information — correction du Framework lui-même hors périmètre de RIF-App (non-objectif PRD §25).

## Historique

- 07.09.2026 — Création du registre, D-01 à D-09 formalisées, D-06 en cours d'arbitrage (voir `docs/decision-llm-multimodal.md`).
- 07.09.2026 — D-06 tranchée (Claude Sonnet 5), D-14 ajoutée (clôture du plan V2), module de contrôle qualité et Route Handlers livrés, module de collecte conditionnelle (ENG-004) livré.
- 07.09.2026 — Orchestrateur conversationnel livré ; renommage genererRenduFlux/corrigerRenduFlux → genererRendu/corrigerRendu ; D-15 ajoutée (extraction structurée du ProjectState via un quatrième outil).
- 07.09.2026 — Première interface (`app/dossiers/`) : liste et création de dossiers, dépôt de sources (rôle choisi par l'utilisateur, pas encore détecté automatiquement), chat connecté à l'orchestrateur conversationnel. Correction en cours de route : le dépôt d'une source n'inscrivait pas la source dans `project_state.sources`, rendant le dépôt invisible à la conversation comme à l'interface — corrigé dans `app/api/dossiers/[dossierId]/sources/route.ts`.
- 08.09.2026 — D-16 ajoutée : `avancerParcours` débloque le parcours, qui ne sortait jamais de BROUILLON.
- 08.09.2026 — PRD V1.3 adopté ; D-17 (Contraintes & Libertés) et D-18 (divergence de nommage) ajoutées.
- 09.09.2026 — D-17 complétée : §24A.3 câblé de bout en bout (proposition par le modèle → attente → autorisation à la confirmation de fiche → effet sur le prompt technique).
- 09.09.2026 — Détection automatique du rôle des sources (PRD §9.1) livrée : `lib/rif/detection-role.ts` classe chaque image déposée par appel multimodal (Claude Sonnet 5), le slot choisi par l'utilisateur devenant un simple indice. Simplification assumée et documentée dans CLAUDE.md : les détections non ambiguës sont auto-confirmées faute de session de revue groupée construite ; seules les détections ambiguës (confiance insuffisante ou désaccord avec le slot) attendent une confirmation individuelle via `PATCH .../sources/[fileId]`.
- 09.09.2026 — Provisioning Supabase préparé (D-02/D-03/D-04) : `supabase/schema-rif-app.sql` complété avec le bucket de stockage privé et ses policies RLS `storage.objects` (nécessaires au fonctionnement — les Route Handlers utilisent le client lié à la session utilisateur, jamais `service_role`, donc sans ces policies le premier dépôt de fichier échoue). Runbook complet dans `docs/provisioning-supabase.md` : création du projet, des deux comptes (Évariste + supervision Alpha_no_code) et des variables d'environnement reste une action utilisateur, jamais exécutée depuis cet environnement.
- 09.09.2026 — Extraction automatique des directives localisées depuis une source annotée (PRD §9.2, ADR-015) livrée : `lib/rif/detection-directives.ts` convertit chaque marque d'annotation détectée sur une image `annotated_source`/`annotated_render` en directive structurée. Correction de fidélité au passage : `ActionDirective` portait 4 valeurs (`preserve`/`modify`/`remove`/`clarify`) sans usage réel dans le code, ne correspondant à aucune des 5 valeurs littérales du PRD (`conserver`/`supprimer`/`remplacer`/`corriger`/`verrouiller`) — remplacé par les 5 valeurs du PRD, sans impact car le champ n'était lu nulle part. `DirectiveLocalisee` gagne un `id` stable (source + rang), nécessaire pour confirmer une directive individuellement (`PATCH .../directives/[directiveId]`). Même scope cut que la détection de rôle : pas de session de revue groupée, confirmation directive par directive pour les cas ambigus (statut `unknown`).
- 12.09.2026 — Audit complet de l'existant face au PRD V2.1 (Neon/Vercel Blob) : aucune modification de code, cf. rapport d'audit de cette date (non conservé dans ce registre).
- 12.09.2026 — Framework recalibré sur le PRD V2.1, V1.4 → V1.6 (ADR-019 : mode `retexturation_contextualisee` et modèle d'environnement `locked`/`editable`/`harmonizable` ; ADR-020 : styles `presentation_naturelle`/`administratif_sobre`/`commercial`) — voir `Framework/99_DOCUMENTATION/99_DECISIONS_ARCHITECTURE.md`. `Exemples/PROJECT_STATE_EXEMPLE.json` refondu sur un cas réel d'Évariste. D-19 ajoutée. Aucun fichier `.ts`/`.tsx` modifié à ce stade.
- 16.09.2026 — Domaine métier TypeScript aligné sur le PRD V2.1 (D-19) : `ModeProduction`/`StyleRendu`/`ElementEnvironnement` d'abord (`lib/rif/project-state.ts` et les modules qui en dépendent), puis la machine à états renommée (`etat-machine.ts` : 13 → 12 états, `SOURCES_ANALYSÉES`, `CONTEXTE_À_CONFIRMER`), répercutée dans les orchestrateurs, les Route Handlers `confirmer`/`reprendre`, `app/dossiers/[dossierId]/page.tsx` et la contrainte `check` de `supabase/schema-rif-app.sql`. 214 tests passent, tsc et build propres.
- 17.09.2026 — Premier pas Vercel (D-19) : `lib/storage/vercel-blob.ts` livré, additif, non branché. Décision d'architecture identifiée pour `depot-neon.ts` : pas d'équivalent direct à une URL signée Supabase côté Vercel Blob pour une lecture navigateur — voie par défaut retenue, une Route Handler qui lit et retransmet. 219 tests passent, tsc et build propres.
- 17.09.2026 — Les 5 vulnérabilités relevées par `npm audit` corrigées : `next` 16.3.0 → 16.3.5 (RCE critique non authentifiée + faille Image Optimization AVIF ; `eslint-config-next` aligné) ; `sharp` résolu comme conséquence (dépendance transitive de `next`) ; `js-yaml` résolu par `npm audit fix` (dépendance transitive d'`eslint`) ; `vitest` 3.2.7 → 5.0.1 (major, nécessite `@types/node` 20 → 24 — aligné sur le Node local réellement utilisé, v24.12.0, plutôt que sur la borne minimale 22 exigée par vitest 5). `vitest.config.ts` renommé `.mts` + `__dirname` → `import.meta.dirname` pour lever un avertissement de compatibilité ESM natif révélé par le passage à vitest 5. `npm audit` : 0 vulnérabilité. 219 tests passent (inchangé), tsc et build propres.
- 17.09.2026 — `depot-neon.ts` + `neon/schema.sql` (D-19) : implémentation Neon de `DepotDossiers`, additive, non branchée. Correction de `lib/storage/vercel-blob.ts` (voir D-19 ci-dessus) : `resolverUrlSignee` ajouté. Schéma sans RLS ni `profiles` (PRD §16.3/§16.5). 223 tests passent, tsc et build propres.
- 17.09.2026 — Bascule réelle sur Neon (D-19) : projet Neon provisionné et lié, `neon/schema.sql` corrigé (référence `neon_auth."user"`, plus de table `users` maison) et exécuté sur la branche `production` réelle, `obtenirDepot()` branché sur `creerDepotNeon`, authentification individuelle livrée via Neon Auth (`lib/auth/{server,client}.ts`, `proxy.ts`, `app/connexion/page.tsx`) et vérifiée contre le service hébergé réel. Compte d'Évariste Blasco créé (`neon_auth."user"`, 1 ligne). Code Supabase propre à RIF-App ensuite supprimé (pas seulement laissé mort) : fichiers, dépendances npm, variables `.env.example`, mentions résiduelles dans les commentaires — hors `supabase/schema.sql` et artefacts V2/n8n, couverts par D-14. 223 tests passent, tsc/build/lint propres, 0 vulnérabilité.
- 18.09.2026 — Pivot geometry-first (D-20, ADR-021) : Framework recalibré V1.6 → V1.7 (REF-001, ARCH-002, LIB-006, LIB-002, `FRAMEWORK_VERSION.md`, `99_CHANGELOG.md`, autorisation exceptionnelle ponctuelle à D-01) ; domaine `lib/rif/geometrie-3d.ts` livré (source modèle 3D, extraction honnêtement non disponible sans fournisseur, pack de contraintes versionné, alignement caméra/photo, catégories de correction) ; `ProjectState` étendu sans migration de base de données (JSONB existant) ; pipeline de dépôt dédié pour la source 3D (token/confirm, contraintes 500 Mo, pas de classification automatique) ; 4e bloc frontend (`DepotSources.tsx`). `canonical_result_id`, dead jusqu'ici, réconcilié avec `generations.is_canonical`. `ModeProduction` non modifié — tension avec les 3 usages geometry-first documentée comme question ouverte, pas tranchée depuis le code. Aucun format 3D ni fournisseur d'extraction choisi.
- 18.09.2026 — Suite de mission : `CategorieCorrection` câblée de bout en bout dans `corrigerRendu` (conversationnel et route HTTP), refus explicite de toute catégorie hors des 6 valeurs documentées — pas de 7e catégorie architecturale ajoutée. `lib/rif/prompt-technique.ts` et `lib/rif/controle-qualite.ts` (`sourceControlePourCritere`) rendus geometry-pack-aware. Audit complet du MASTER BACKEND COMPLETION LOOP précédent (DONE/PARTIAL/NOT_DONE par item, aucun superseded) ; corrections des gaps bon marché (format d'erreur uniforme sur 2 routes, agrégats dashboard affichés, statuts 3D/pack/alignement affichés dans la fiche projet) ; gaps plus lourds (reload générations après refresh, UI comparaison/canonique, déclenchement audit multimodal) documentés explicitement dans le nouveau `docs/RIF_BACKEND_CONTRACTS_V2.md`, pas laissés silencieux. Aucune migration SQL nécessaire. 276 tests passent, tsc/build/lint propres.
- 17.09.2026 — Upload direct navigateur → Vercel Blob (PRD §16.1, D-19) : corrige le 413 constaté en production (limite 4,5 Mo des Vercel Functions). Nouvelle route `.../sources/token` (jeton client contraint), `DepotSources.tsx` téléverse en direct puis n'envoie qu'un JSON de métadonnées, `.../sources` relit le blob pour la détection de rôle. `lib/storage/contraintes-source.ts` (testé) centralise les contraintes. `enregistrerFichierSource` ne réuploade plus. 225 tests passent, tsc/build/lint propres.
- 17.09.2026 — Persistance de l'historique de conversation (PRD §13.5, D-19) : table `messages` (texte seul), `DepotDossiers` étendue, `executerTourConversationnel` charge/persiste via un point de sortie unique, `GET .../message` ajouté, `ConversationRif.tsx` n'envoie plus l'historique. Vérifié contre la base Neon réelle. Reste ouvert : `BLOB_READ_WRITE_TOKEN` manquant. 221 tests passent, tsc/build/lint propres.
- 17.09.2026 — Parsing robuste des réponses API côté client : `lib/rif/reponse-client.ts::lireReponseApi` corrige « Unexpected end of JSON input » affiché après un 413 (DepotSources.tsx faisait `reponse.json()` sans vérifier `reponse.ok`). Câblé sur les 3 appels de `DepotSources.tsx` ; même bug non corrigé ailleurs (4 fichiers), signalé. 231 tests passent, tsc/build/lint propres.
- 17.09.2026 — Migrations SQL versionnées (D-19) : audit read-only préalable (schéma Neon 100 % conforme à `neon/schema.sql`, rien de manquant — seul le suivi manquait). `neon/migrations/0001_initial_schema.sql` (copie exacte, vérifiée par introspection), `lib/neon/migrations.ts` (+ testé) et `migrations-executeur.ts` (une transaction HTTP par migration, jamais à moitié appliquée), `scripts/db-migrate.ts` (Node natif, `tsconfig.json` gagne `allowImportingTsExtensions`). `npm run db:migrate`/`db:migrate:status`, non connecté au build Vercel. `db:migrate` non exécuté pour de vrai (reste ouvert). 240 tests passent, tsc/build/lint propres.
- 17.09.2026 — `npm run db:migrate` exécuté pour de vrai contre `production` : `0001_initial_schema.sql` appliquée, `schema_migrations` en fait foi, comptages métier vérifiés inchangés.
- 17.09.2026 — Durcissement de l'exécuteur de migrations (D-19) : le découpage naïf sur `;` (fragile pour `DO $$…$$`, fonctions, triggers, chaînes contenant `;`) remplacé par le `Client` WebSocket de `@neondatabase/serverless` — vraie session Postgres, fichier de migration exécuté tel quel via le protocole « simple query ». Vérifié en direct contre Neon (DO block, fonction, trigger, rollback complet sans trace) avant d'écrire le code définitif ; `migrations-executeur.test.ts` (nouveau, `Client` mocké) couvre la séquence BEGIN/migration/insert/COMMIT et le comportement d'échec (ROLLBACK, non-écriture de schema_migrations, connexion toujours fermée). 243 tests passent, tsc/build/lint propres.
