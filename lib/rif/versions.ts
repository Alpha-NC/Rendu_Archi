/**
 * Versions enregistrées sur chaque dossier créé (PRD §13.1, §21 : « Chaque
 * génération est rattachée aux versions exactes du Framework, du package,
 * du prompt et des modèles »).
 */

/** rif-framework/FRAMEWORK_VERSION.md — à synchroniser manuellement si le Framework est mis à jour. */
export const FRAMEWORK_VERSION = 'V1.4'

/**
 * Version du package d'implémentation RIF-App lui-même
 * (rif-framework/Implementations/RIF-App/). Aucun schéma de version formel
 * n'existe encore pour ce package — valeur provisoire, à remplacer dès
 * qu'un vrai schéma de release est défini.
 */
export const IMPLEMENTATION_VERSION = 'RIF-App-0.1.0'
